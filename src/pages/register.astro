---
import Layout from "@/layouts/Layout.astro";
import { RegisterForm } from "@/components/auth/RegisterForm";

// Obsługa sesji - middleware już sprawdził czy użytkownik jest zalogowany
// Jeśli jest zalogowany, zostanie automatycznie przekierowany do dashboardu
---

<Layout title="Rejestracja - Paw Notes" hideHeader>
  <div class="min-h-screen flex items-center justify-center p-4 bg-background">
    <RegisterForm client:load />
  </div>
</Layout>
